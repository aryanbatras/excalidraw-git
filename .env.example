# Auth.js (required)
AUTH_SECRET=                # openssl rand -base64 32
AUTH_GITHUB_ID=            # GitHub OAuth App client id
AUTH_GITHUB_SECRET=        # GitHub OAuth App client secret

# Optional: GitHub PAT for direct API access (bypasses OAuth session token)
# Get one at https://github.com/settings/tokens with repo scope
GITHUB_TOKEN=
